# No environment variables are required.
#
# If client-side configuration is added later, Vite only exposes variables whose
# names start with VITE_. Never store secrets in those variables: they are bundled
# into the JavaScript delivered to every visitor.
